
pipeline {
agent any

stages {
    stages('compile') {
        steps {
            sh 'javac addition.java'
      } 

}
    stages('run') {
        steps {
            sh 'java addition'
        }
    }
  }
}
